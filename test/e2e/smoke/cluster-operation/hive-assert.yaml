apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: test-hive-metastore-default
spec:
  template:
    spec:
      containers:
        - name: metastore
status:
  availableReplicas: ($available_replicas)
  replicas: ($available_replicas)
