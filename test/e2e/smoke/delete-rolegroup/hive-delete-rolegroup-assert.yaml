apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: test-hive-metastore-primary
spec:
  template:
    spec:
      containers:
        - name: metastore
status:
  availableReplicas: ($available_replicas)
  currentReplicas: ($available_replicas)
  replicas: 1
