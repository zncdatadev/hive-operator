apiVersion: v1
kind: Service
metadata:
  name: {{ include "hive-operator.fullname" . }}
  labels:
    {{- include "hive-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hive-operator.selectorLabels" . | nindent 4 }}
